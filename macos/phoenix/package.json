{
  "name": "phoenix-config",
  "private": true,
  "scripts": {
    "build": "bun build phoenix.ts --outfile ~/.phoenix.js"
  },
  "dependencies": {
    "@types/phoenix": "github:mafredri/phoenix-typings#main"
  }
}
